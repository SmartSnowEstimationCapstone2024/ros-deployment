from setuptools import setup

setup(
    name="snowcap_python",  # The name of your Python package
    version="0.0.0",
    packages=["snowcap_python"],  # Make sure this matches your module name
    install_requires=[],  # Add dependencies if needed
)